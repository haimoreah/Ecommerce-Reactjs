# مهمة: Instagram Reach Operating System

أنت Claude Code على جهاز محلي، وعندك وصول شبكة كامل وملفات محلية —
هذه المهمة انتقلت إليك من جلسة سحابية معزولة كانت نطاقات Meta محجوبة فيها.

## الريبو
`haimoreah/Ecommerce-Reactjs` — الفرع `claude/instagram-reels-growth-system-vlb926`

```bash
git clone <repo> && cd Ecommerce-Reactjs
git checkout claude/instagram-reels-growth-system-vlb926
```

## المبني حاليًا

**سكِل كامل** في `.claude/skills/instagram-reach/` — اقرأ `SKILL.md` أولًا،
فيه بوابة نشر (≥85 موافقة)، وقواعد معرفة مقسّمة إلى
CONFIRMED / STRONG EVIDENCE / HYPOTHESIS بمصادر وتواريخ.

**٧ وكلاء** في `.claude/agents/` — أربعة يقسّمون الـ100 نقطة بالضبط،
`reel-data` للأرقام، `reel-audit` للحساب، `reel-skeptic` محامي شيطان إلزامي قبل أي موافقة.

**٣ سكربتات** في `scripts/`:
- `analyze.mjs` — معايير وأنماط ومعايرة (يرفض الحكم تحت n=10)
- `fetch-ig.mjs` — يسحب الريلات من Graph API (يحتاج `IG_ACCESS_TOKEN`)
- `publish-reel.mjs` — ينشر ريل (يحتاج `--confirm`)

## الحقائق المثبتة — لا تعيد التحقيق فيها

1. **Account Status أخضر بالكامل.** لا عقوبة، ولا قيود، و«no limits to your reach».
   أي تشخيص يفترض عقوبة = خطأ.
2. **الريلات الثلاثة في `data/reels.csv` كلها مموّلة (ads).** لا توجد بيانات عضوية إطلاقًا.
   متوسط المشاهدة ~1.3 ثانية فيها **طبيعي لانطباعات إعلانية باردة** ولا يدل على ضعف الهوك.
3. **عدم تطابق الجمهور:** المتابعون جاؤوا من محتوى أغاني/رقص، والمحتوى الآن AI/شطرنج/اقتصاد.
   الجمهور 93.3% رجال.
4. **أقوى إشارة متاحة:** R-A حقق **6 أضعاف** معدل مشاركة R-C على وصول متطابق (فرق 0.7%).
   بما أن التوزيع مدفوع ومتساوٍ، الفرق من **المحتوى**. R-A هو القالب.
5. سبق نشر محتوى منسوخ من حسابات أخرى، وأحدها احتوى معلومة خاطئة عن Claude
   («Slash Ghost» ليس أمرًا حقيقيًا).

## المطلوب — بالترتيب

**١. افحص R-A.** ما موضوعه؟ ما هوكه؟ لماذا أرسله الناس؟
هذا السؤال يستحق أكثر من 20 ريل جديد.

**٢. اسحب البيانات الحقيقية.** ساعد المستخدم في:
```
developers.facebook.com → Create App (Business)
→ Instagram → API setup with Instagram business login
→ Generate token
```
لا يحتاج صفحة فيسبوك، ولا App Review (Standard Access يكفي لحسابه).
ثم `IG_ACCESS_TOKEN=... node .claude/skills/instagram-reach/scripts/fetch-ig.mjs`

**٣. علّم المموّل يدويًا.** الـAPI لا يكشف أي ريل عليه Boost.
بدون عمود `promoted=yes` الصحيح، كل معيار سيخرج خاطئًا.

**٤. الأولوية القصوى: 5 ريلات عضوية بلا ترويج.** لا يوجد بديل عن هذه الخطوة.
كل تحليل قبلها تخمين.

## قيود ملزمة

- **لا ترقيم مخترع.** لا أوزان إشارات («المشاركة = 5 لايكات» رقم لا مصدر له).
- **لا تشخيص «Shadowban».** غير موثّق — افحص Account Status بدلًا منه.
- **لا تخلط مموّل بعضوي** في أي معيار.
- **الخلية الفارغة ليست صفرًا.** «لم نقس» ≠ «قسنا صفرًا».
- **افصل مستويات المعرفة** دائمًا، واذكر «غير مثبت — يحتاج Test» عند عدم اليقين.
- **لا تجامل.** الهدف منع المحتوى الضعيف من الوصول إلى Publish.

اقرأ `references/knowledge-base.md` قبل أي ادعاء عن الخوارزمية — ولا تعتمد على ذاكرتك.
